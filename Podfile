# Uncomment this line to define a global platform for your project
platform :ios, '8.0'
# Uncomment this line if you're using Swift
# use_frameworks!
link_with 'BlockDump', 'BlockDump Tests'

target 'BlockDump' do
    pod 'HTPressableButton', :git => 'https://github.com/herinkc/HTPressableButton.git'
end

target 'BlockDumpTests' do
    pod 'HTPressableButton', :git => 'https://github.com/herinkc/HTPressableButton.git'
end

